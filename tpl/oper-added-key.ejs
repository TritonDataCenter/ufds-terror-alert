Dear operator,

An operator account on <%= cloud_name %> has added a new public key:

Time of event: <%= when.toISOString() %>

Account UUID: <%= user.uuid %>
Account login: <%= user.login %>
Member of cn=operators: <%= (user.operator === 1) ? 'yes' : 'no' %>
Member of cn=readers: <%= (user.reader === 1) ? 'yes' : 'no' %>
Email address: <%= user.email %>

Key name: <%= name %>
Key comment: <%= key.comment %>
Key type and size: <%= key.size %> bit <%= key.type %>
Key fingerprint (MD5): <%= key.fingerprint('md5').toString() %>
Key fingerprint (SHA256): <%= key.fingerprint().toString() %>
Key action: add

This key also appears in UFDS in the following user accounts:
<% otherLocations.forEach(function (loc) { %>
 * <%= loc.login %> (<%= loc.email %>)
     Key name: <%= loc.name %>
     Comment: <%= loc.comment %>
<% }); %>
